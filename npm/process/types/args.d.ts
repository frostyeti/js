/**
 * The current process arguments. The arguments do not include the
 * executable path or the script path.
 * @example
 * ```typescript
 * import { args } from '@frostyeti/process/args';
 *
 * console.log(args);
 * ```
 */
export declare const args: ReadonlyArray<string>;
